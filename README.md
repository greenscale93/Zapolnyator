# Zapolnyator — автоматическое заполнение отчёта ДКП из выгрузки 1С

**Zapolnyator** — это система, которая принимает через Telegram два Excel-файла (шаблон отчёта и выгрузку данных) и автоматически заполняет листы отчёта по заданным правилам. Агент не использует LLM — всё работает по жёстким правилам, описанным в JSON-конфиге. Подходит для ежемесячного формирования отчёта департамента корпоративных продаж.

## Особенности

- Принимает Excel-файлы через Telegram-бота.
- Заполняет листы «Реализация», «ДС», «ФОТ» (с группировкой по сотрудникам и разделением окладов/премий).
- Обрабатывает лист «Взаиморасчеты» со схлопыванием дублей, разделением по БДР/БДДС и маппингом офисов.
- Записывает **ФактическийФОТ** в ячейку на листе сводки (с исключением Премии по Сорокину).
- Читает значения из сводного листа после пересчёта формул (**Прибыль отдела** и другие показатели).
- Режим **диагностики** (`/diagnostic`) — показывает координаты ячейки для каждого показателя.
- Фильтрует данные по сценарию «ФАКТ», исключает подразделение «ГКП 10.6 (Емельянова)» и ВГО-строки.
- Автоматически определяет месяц и год из имени файла выгрузки.
- Поддерживает группировку и агрегацию (суммирование, объединение комментариев).
- Очищает числовые значения от неразрывных пробелов (`\xa0`) и других мусорных символов.
- Работает без LLM — только правила и код.
- Режим автотеста (`/autotest`) — запоминает последние файлы и автоматически запускает обработку при `/start`.
- Все сервисы — в Docker, развёртывание на VPS одной командой.

## Архитектура

Система состоит из трёх микросервисов, каждый в отдельном контейнере:

| Сервис | Назначение | Технологии |
|---|---|---|
| **Telegram Gateway** | Приём файлов, управление диалогом, отправка сообщений и готового файла | aiogram, aiohttp, Redis |
| **Orchestrator** | Координация процесса: чтение правил, вызов Worker, управление сессиями, отправка уведомлений | FastAPI, Redis, httpx |
| **Worker** | Выполнение основной работы: Excel (запись через openpyxl + LibreOffice, чтение через calamine) | FastAPI, openpyxl, pandas, LibreOffice, python-calamine |

Связь между сервисами — по REST API через внутреннюю сеть Docker. Состояние задач хранится в Redis.
Связь между сервисами — по REST API через внутреннюю сеть Docker. Состояние задач хранится в Redis.

Требования
Docker и Docker Compose (для развёртывания)

Python 3.11+ (для локальной разработки)

Telegram Bot Token (получить у @BotFather)

(Опционально) учётная запись Docker Hub для избежания лимитов

Установка и запуск
Клонирование репозитория
bash
git clone git@github.com:greenscale93/Zapolnyator.git
cd Zapolnyator
Переменные окружения
Создайте файл .env (или задайте переменные через docker-compose.yml):

env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TEMP_DIR=/app/temp
ORCHESTRATOR_URL=http://orchestrator:8000
WORKER_URL=http://worker:8000
REDIS_URL=redis://redis:6379
AUTO_TEST=true   # опционально
AUTO_START_CHAT_ID=123456789   # опционально, chat_id для автозапуска
Запуск через Docker Compose
bash
docker compose up -d --build
Сервисы будут доступны:

Telegram Gateway — порт 8000 (для внешних вызовов, не используется напрямую)

Orchestrator — порт 8001 (внутренний)

Worker — порт 8002 (внутренний)

Redis — порт 6379 (внутренний)

Локальная разработка
Если вы хотите запускать сервисы вне Docker:

Установите зависимости для каждого сервиса (в папках telegram-gateway, orchestrator, worker):

bash
pip install -r requirements.txt
Запустите Redis локально (или укажите другой URL).

Запустите каждый сервис отдельно:

bash
# в папке telegram-gateway
python -m src.main
# в папке orchestrator
uvicorn src.main:app --host 0.0.0.0 --port 8001
# в папке worker
uvicorn src.main:app --host 0.0.0.0 --port 8002
Использование
Команды бота
/start — начать работу (при включённом автотесте сразу запускает обработку последних файлов).

/reset — сбросить состояние.

/autotest — включить/выключить режим автотеста (сохраняется индивидуально для пользователя).

/diagnostic — включить/выключить режим диагностики (показывает координаты ячеек в сообщениях).

/stop — принудительно остановить текущую задачу.

Загрузка файлов
Отправьте боту шаблон отчёта (Excel-файл, например, ДКП 10 - Май 2026.xlsx).

Отправьте выгрузку данных (Excel-файл с именем, содержащим ВыгрузкаДляExcel, например, ВыгрузкаДляExcel_2026-06.xlsx).

Месяц и год определяются автоматически из имени файла (год — 4 цифры, месяц — 2 цифры после дефиса или подчёркивания).

Бот применит правила из rules.json и вернёт заполненный файл.

Порядок отправки не важен. Можно отправить файлы одновременно.

Настройка правил заполнения (rules.json)
Все правила хранятся в файле orchestrator/rules.json. Он имеет следующую структуру:

```json
{
  "sheets": {
    "ИмяЛиста": {
      "source_columns": {
        "номер_колонки_в_шаблоне": "имя_колонки_в_источнике"
      },
      "append_to_end": true,
      "header_row": 2,
      "filters": { ... },
      "exclude_filters": { ... },
      "group_by": ["колонка1"],
      "aggregations": { "колонка": "sum | concat | concat_premia" },
      "custom_processing": {
        "type": "vzaimoraschety",
        "direction_mapping": { ... },
        "special_office_keyword": "...",
        "special_office_replace": { ... }
      }
    }
  },
  "write_values": [
    {
      "key": "ffot",
      "type": "ffot",
      "label": "ФОТ фактический, руб.",
      "row_name": "ФОТ фактический, руб.",
      "row_column": 3,
      "month_row": 2,
      "month_offset": -1,
      "exclude_employee": {
        "Сорокин Илья Вячеславович": ["Премия"]
      }
    }
  ],
  "read_values": [
    {
      "key": "profit",
      "label": "Прибыль отдела, мес, к зачету",
      "row_name": "Прибыль отдела, мес, к зачету",
      "row_column": 3,
      "month_row": 2,
      "month_offset": 0
    }
  ]
}
```

### write_values
Значения, которые вычисляются из данных выгрузки и записываются в шаблон.
- `type: "ffot"` — суммирует колонку Сумма по строкам с ТипЗаписи=ФактическийФОТ
- `month_offset: -1` — запись в колонку предыдущего месяца
- `exclude_employee` — исключения по сотрудникам (для Сорокина Премия не учитывается)

### read_values
Значения, которые читаются из шаблона после заполнения и пересчёта формул, и отправляются в Telegram.
- `month_offset: 0` — чтение из колонки текущего месяца
- Координаты ячейки показываются в режиме диагностики (`/diagnostic`)

Поддерживаемые агрегации
sum — суммирование числовых значений.

concat — объединение строк через "; ".

concat_premia — объединение комментариев только из строк с видом начисления «Премия» (используется для листа ФОТ).

Пример для листа «Реализация»
json
"Реализация": {
  "source_columns": {
    "3": "Подразделение",
    "4": "Сценарий",
    "5": "{month} {year}",
    "6": "Контрагент",
    "7": "Проект",
    "8": "СуммаСНДС",
    "9": "СуммаБезНДС",
    "10": "СтавкаНДС",
    "11": "Этап",
    "12": "НомерК7"
  },
  "append_to_end": true,
  "header_row": 2,
  "filters": {
    "Сценарий": "ФАКТ",
    "ТипЗаписи": "Реализация"
  },
  "exclude_filters": {
    "Подразделение": "ГКП 10.6 (Емельянова)"
  }
}
Разработка и доработка
Как добавить новый лист
Добавьте новый объект в sheets в rules.json.

Укажите:

source_columns — соответствие колонок шаблона и источника.

filters и exclude_filters при необходимости.

Если нужна группировка — укажите group_by и aggregations.

Перезапустите контейнеры (или просто замените файл и перезапустите Orchestrator).

Как изменить логику обработки данных
Основная логика обработки распределена по модулям Worker-сервиса:

- **`src/excel_processor.py`** (`apply_sheet_mapping`) — низкоуровневая работа с Excel: фильтрация, группировка, вставка данных в шаблон.
- **`src/excel_processor.py`** (`_save_and_fix_formats`) — сохранение с фиксом форматов: openpyxl → LibreOffice --convert-to.
- **`src/ffot_writer.py`** — расчёт и запись ФактическийФОТ, чтение read_values из шаблона.
- **`src/vz_processing.py`** — специфическая предобработка взаиморасчетов перед вставкой.
- **`src/template_reader.py`** — чтение данных из шаблона через python-calamine.
- **`src/business_logic/`** — вспомогательная бизнес-логика (разрешение колонок, фильтрация, группировка).

### Excel: гибридная архитектура

| Операция | Инструмент | Почему |
|---|---|---|
| **Запись** (вставка строк, запись ячеек) | openpyxl | Единственная Python-библиотека для модификации XLSX |
| **Сохранение + фикс форматов** | LibreOffice `--convert-to xlsx` | Исправляет форматы дат и ячеек, портящиеся при openpyxl.save() |
| **Конвертация дат в текст** | openpyxl (перед save) | Все datetime-ячейки → "Май 2026" (текст), чтобы формулы не ломались |
| **Чтение** (офисы, названия строк, значения) | python-calamine | Быстрый Rust-парсер, не модифицирует файл |
| **Чтение выгрузки** | pandas + openpyxl | Обычный pd.read_excel() |

Для добавления нового типа обработки достаточно создать новый модуль в `worker/src/` и подключить его в нужном месте цепочки.

Планы по развитию

- Реализовать вычисление итоговых показателей (метрики) и запись их в отчёт.
- Поддержка большего числа типов выгрузок (например, CSV).
- Веб-интерфейс для настройки правил.

## 📁 Структура проекта

```
Zapolnyator/
├── .github/workflows/deploy.yml   # GitHub Actions — автодеплой на VPS
├── orchestrator/                   # Микросервис координации
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── rules.json                  # Правила заполнения листов (главный конфиг)
│   └── src/
│       ├── __init__.py
│       ├── main.py                 # Точка входа (FastAPI)
│       ├── api.py                  # REST-эндпоинты для Telegram Gateway
│       ├── api_models.py           # Pydantic-модели запросов
│       ├── agent.py                # Координатор: применяет rules.json, вызывает Worker
│       ├── agent/
│       │   └── mapping_handler.py  # Управление маппингом офисов (inline-клавиатуры, CRUD)
│       ├── telegram_notifier.py    # Отправка сообщений/документов в Telegram
│       ├── session_manager.py      # Работа с Redis (состояния задач, автотест)
│       ├── memory.py               # Хранение правил и исключений в SQLite
│       ├── mapping_store.py        # CRUD маппингов контрагент→офис в SQLite
│       └── worker_client.py        # HTTP-клиент для вызова Worker
├── telegram-gateway/               # Микросервис Telegram-бота
│   ├── Dockerfile
│   ├── requirements.txt
│   └── src/
│       ├── __init__.py
│       ├── main.py                 # Точка входа (aiogram)
│       ├── handlers/
│       │   ├── __init__.py         # Регистрация всех роутеров
│       │   ├── commands.py         # Команды: /start, /reset, /autotest, /stop
│       │   ├── documents.py        # Загрузка и сохранение Excel/MXL-файлов
│       │   ├── polling.py          # Мониторинг статуса задачи
│       │   ├── callbacks.py        # Обработка inline-кнопок
│       │   └── fsm.py              # FSM-состояния и ответы на вопросы
│       ├── utils/
│       │   ├── __init__.py
│       │   └── file_storage.py     # Утилиты: последние файлы, дата из имени
│       └── client.py               # HTTP-клиент для вызова Orchestrator
├── worker/                         # Микросервис вычислений
│   ├── Dockerfile
│   ├── requirements.txt
│   └── src/
│       ├── __init__.py
│       ├── main.py                 # Точка входа (FastAPI), инструменты для Orchestrator
│       ├── mxl_parser.py           # Парсинг MXL-файлов 1С
│       ├── excel_processor.py      # Чтение Excel, фильтрация, группировка, запись
│       ├── vz_processing.py        # Предобработка взаиморасчетов (схлопывание, маппинг)
│       ├── vz_utils.py             # Поиск контрагентов без маппинга
│       ├── template_reader.py      # Чтение списка офисов из шаблона
│       ├── business_logic/
│       │   ├── __init__.py
│       │   ├── column_mapper.py    # Определение колонок (авто + user_mapping)
│       │   ├── filtering.py        # Фильтрация строк (ФАКТ, исключения, ВГО)
│       │   ├── payroll_processor.py # Группировка ЗП по сотрудникам
│       │   ├── settlement_processor.py # Обработка взаиморасчетов (3 ветки)
│       │   ├── ffot_processor.py   # Расчёт ФактическийФОТ
│       │   └── process_data.py     # Оркестратор шагов обработки
│       ├── file_utils.py           # (зарезервировано)
│       └── metrics.py              # (зарезервировано)
├── data/                           # Монтируемая директория для файлов
├── docker-compose.yml
├── .env.example
├── DEPLOYMENT.md                # Инструкция по деплою и диагностике
└── README.md
```

## Ключевые файлы

### Конфигурация
- **`rules.json`** — основной конфигурационный файл. Определяет:
  - `sheets` — какие листы заполнять, куда копировать, фильтры, группировки
  - `write_values` — значения для вычисления и записи (ФОТ)
  - `read_values` — значения для чтения и вывода в Telegram

### Orchestrator
- **`src/agent.py`** — ядро координации. Читает `rules.json`, последовательно вызывает Worker, отправляет уведомления.
- **`src/agent/mapping_handler.py`** — маппинг офисов для взаиморасчетов.
- **`src/telegram_notifier.py`** — отправка сообщений и документов в Telegram.
- **`src/session_manager.py`** — Redis-состояния задач, автотест, диагностика.
- **`src/api_models.py`** — Pydantic-модели запросов REST API.

### Worker
- **`src/excel_processor.py`** — главный модуль Excel: `apply_sheet_mapping()` (вставка данных), `_save_and_fix_formats()` (сохранение через LibreOffice), `read_excel_structure()`.
- **`src/ffot_writer.py`** — расчёт ФактическийФОТ, запись в ячейку, чтение read_values. Используется для write_values/read_values из rules.json.
- **`src/template_reader.py`** — чтение шаблона через python-calamine: поиск строк, колонок, чтение ячеек.
- **`src/vz_processing.py`** — предобработка взаиморасчетов: направление, БДР/БДДС, маппинг офисов, комментарии.
- **`src/mxl_parser.py`** — парсинг MXL-файлов 1С.
- **`src/excel_processor.py`** — выполняет всю работу с Excel: чтение данных, фильтрацию, группировку с агрегацией, очистку чисел, запись в целевой файл. Реализована логика `concat_premia` и очистка от неразрывных пробелов.
- **`src/vz_processing.py`** — предобработка взаиморасчетов: вычисление направления, разделение по БДР/БДДС, заполнение пустых маппингов, схлопывание дублей.
- **`src/business_logic/process_data.py`** — оркестратор полного цикла обработки данных: определение колонок → фильтрация → группировка → payroll → взаиморасчеты → ФОТ.
- **`src/vz_utils.py`** — поиск контрагентов с пустым маппингом офиса.
- **`src/template_reader.py`** — чтение списка офисов из листа «Отчетность БИТ 2026» шаблона.

### Telegram Gateway
- **`src/handlers/`** — пакет обработчиков, разбитый на модули:
  - `commands.py` — команды `/start`, `/reset`, `/autotest`, `/stop`, `/edit_mapping`
  - `documents.py` — приём и сохранение Excel/MXL-файлов
  - `polling.py` — фоновый мониторинг статуса задачи
  - `callbacks.py` — обработка inline-кнопок (выбор офиса, удаление маппинга)
  - `fsm.py` — состояния ожидания ответа пользователя
- **`src/utils/file_storage.py`** — сохранение/загрузка последних файлов для автотеста, извлечение месяца и года из имени файла.
