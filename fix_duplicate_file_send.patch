diff --git a/orchestrator/src/agent.py b/orchestrator/src/agent.py
index 1234567..abcdefg 100644
--- a/orchestrator/src/agent.py
+++ b/orchestrator/src/agent.py
@@ -119,10 +119,8 @@ class OrchestratorAgent:
             first_sheet = False

         if output_path:
-            await self._send_telegram_message(
-                user_id,
-                f"✅ Обработка завершена! Файл сохранён.",
-                file_path=output_path
+            await self._send_telegram_message(user_id,
+                "✅ Обработка завершена! Файл будет отправлен."
             )
             await self.session_manager.update_session(task_id, {
                 "status": "done",